using Godot;
using System;

public partial class Menu : Node2D {
    override public void _Ready() {
        // основное меню, кнопка старта игры и дополнительная информация

        // Connect the button to the method
        // GetNode<Button>("Button").Connect("pressed", this, nameof(OnButtonPressed));
    }
}
